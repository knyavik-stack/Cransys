# Changelog (История изменений) Cransys

Все изменения проекта документируются в данном файле.

## [1.1.0] — 2026-09-16
### Киберзащита, подтверждение Email, сохранение истории аудитов и светлая тема
- **Персистентная история аудитов (`lib/db/audit-store.ts`):**
  - Развернуто централизованное хранилище аудитов, сохраняющее результаты в Neon PostgreSQL с локальным отказоустойчивым JSON-fallback.
  - В запросы аудитов (`DropZone.tsx` и `page.tsx`) внедрена передача `x-user-id` и `x-user-email` для гарантированного связывания отчетов с аккаунтом.
  - В личном кабинете (`/dashboard`) история аудитов загружается по пользователю с поддержкой детального просмотра каждого сохраненного отчета (`/api/audit/[id]`).
- **Киберзащита паролей (`lib/auth/password-validator.ts`):**
  - Установлены требования кибербезопасности: минимум 8 символов, латинские буквы, обязательное наличие заглавных (A-Z), строчных (a-z) и цифр (0-9).
  - Интерактивный UI-валидатор пароля с индикацией каждого правила в реальном времени при регистрации (`/sign-up`) и сбросе пароля (`/forgot-password`).
- **Двухэтапная регистрация с подтверждением Email:**
  - На шаге 1 пользователь вводит данные и защищенный пароль, система создает учетную запись в базе данных со статусом ожидания верификации (`emailVerified: false`) и генерирует 6-значный код подтверждения (`/api/auth/register`).
  - На шаге 2 отображается форма ввода кода подтверждения (`/api/auth/verify-email`) с возможностью повторной отправки (`/api/auth/resend-code`).
  - После успешного ввода кода статус обновляется на `emailVerified: true`, и пользователь авторизуется в системе.
  - В панели администратора (`/admin`) отображаются все зарегистрированные пользователи с бейджем «Email подтвержден» или «Ожидает кода».
- **Дизайн-система (100% Светлая тема):**
  - Блок «Оцените глубину аудита на эталонном отчете» на главной странице переведен в чистую светлую тему (белоснежный фон, контрастная серая типографика, изумрудные акценты).
  - Подвал сайта (`components/Footer.tsx`) полностью переведен в светлую тему в соответствии с дизайн-макетами.
  - Устранены несоответствия стилей шапки и навигации.

## [1.0.1] — 2026-09-15
### Исправление загрузки чанков и перезапуск Dev-сервера
- Устранен сбой загрузки клиентских чанков `ChunkLoadError` на главной странице: перезапущен dev-сервер со свежей сборкой Next.js 15.
- Проведена полная верификация типов и сборки (`lint_applet` и `compile_applet` — успешно).

## [1.0.0] — 2026-09-15
### Полнофункциональная Админка, CRUD пользователей, Конфигуратор тарифов и Защита отчетов
- **Полноценное управление пользователями в Панели Администратора (`/admin`):**
  - Создан бэкенд и UI для полного цикла управления учетными записями (`/api/admin/users`).
  - Возможность блокировки/разблокировки пользователей в один клик.
  - Удаление пользователей с модальным окном подтверждения.
  - Редактирование email, пароля, имени, лимитов отчетов, выручки и тарифа.
  - Ручное создание новых пользователей прямо из панели администратора.
- **Конфигуратор тарифов (`/api/admin/tiers`):**
  - Администратор может вручную изменять цены в рублях, лимиты проверок и включать/выключать модули (Direct API, AI-инсайты, кластеризация запросов, White-label PDF, корпоративная автоматизация) с моментальным сохранением в персистентную базу.
- **Реальная база пользователей и аналитика:**
  - Заменена имитация на реальное персистентное хранилище (`lib/db/users-store.ts` и `lib/db/tiers-store.ts`).
  - Все финансовые метрики (выручка, конверсии, средний чек, количество проверок) рассчитываются строго по реальным зарегистрированным пользователям.
- **Разграничение ролей и маршрутизация:**
  - Администратору закрыт доступ в клиентский дашборд с предупреждением и прямой кнопкой перехода в `/admin`.
  - В шапку сайта добавлена навигационная ссылка «Панель Admin» для учетных записей администраторов.
- **Защита отчетов и Paywall-гейтинг (`AuditResults.tsx`):**
  - Пользователи без оплаченного тарифа видят только поверхностные метрики (Индекс здоровья, сумму сливов, расход и число кампаний).
  - Детализация правил, факты, рекомендации, графики и экспорт PDF заблокированы и заблюрены с баннером оформления тарифа.
  - Для тестового суперюзера (`TESTER_ADMIN`) развернут 0 ₽ Sandbox переключатель всех 5 тарифов прямо в шапке отчета с полным доступом ко всем функциям без ограничений.
- **Полноценный контур авторизации:**
  - Реализована строгая валидация паролей при входе (`/api/auth/login`).
  - Добавлена страница восстановления пароля (`/forgot-password` и `/api/auth/reset-password`).
  - Добавлена регистрация новых пользователей (`/sign-up` и `/api/auth/register`).

## [0.9.6] — 2026-09-15
### Исправление гидратации и загрузки чанков в панели администратора
- Оптимизирован жизненный цикл рендеринга `app/admin/page.tsx` с использованием паттерна `useSyncExternalStore` для безопасного монтирования на клиенте.
- Устранена ошибка `ChunkLoadError` и предотвращен таймаут асинхронной загрузки чанков Recharts.

## [0.9.5] — 2026-09-15
### Безопасность авторизации, Панель Супер-Администратора и УТП Независимого движка
- Удалены публичные кнопки тестового входа.
- Учетные данные вынесены в серверные переменные окружения.
- Секция «Что проверяет независимый движок Cransys» на главной странице.
- Первоначальный каркас админки.

## [0.9.0] — 2026-09-15
### 5-уровневая тарифная сетка, Лимиты и Гейтинг API (Step 7.1)
- 5 тарифов: `EXPRESS_SINGLE`, `EXPRESS_PACK`, `PRO`, `MAX`, `CORP`.
- Гейтинг Direct API от тарифа PRO.
- Витрина тарифов на главной странице.

## [0.7.0] — 2026-09-15
### Глубокая аналитика поисковых запросов и AI-минусация (Step 6)
- Модули `lib/ai/search-query-analyst.ts` и `lib/ai/rsya-blacklist.ts`.
- Компонент `SearchQueryVisualizer.tsx`.
- Документ `AI_FUTURE_ROADMAP.md`.
