# 📌 Статус проекта Cransys

**Дата обновления:** 17 сентября 2026 г.  
**Текущая фаза:** Спринт 2 (Глобальная консистентность интерфейса, Сквозной Footer, Синхронизация тарифов, Правовой комплаенс 152-ФЗ и Аудит активности пользователей)  
**Репозиторий:** `knyavik-stack/cransys`  
**Стек:** Next.js 15 (App Router, React 19, TypeScript), Tailwind CSS, Server-Side Data Stores, Neon PostgreSQL (Drizzle ORM), Nodemailer (Yandex SMTP), Yandex Direct API v5 OAuth, ЮKassa.

---

## 🎯 Текущий прогресс
- [x] Инициализирована архитектурная и файловая иерархия проекта по спецификации v2.
- [x] Настроен `.cursorrules` для соблюдения стандартов vibe-coding.
- [x] Подготовлены GitHub Actions CI/CD воркфлоу (`.github/workflows/ci.yml`, `preview.yml`).
- [x] Развернуты обязательные файлы трекинга: `_СТАТУС.md`, `changelog.md`, `_ПРОМПТЫ_ИСТОРИЯ.md`.
- [x] Создана папка дизайн-системы и макетов `maket/`.
- [x] Развернута базовая модульная структура (`lib/audit/`, `db/`, `components/`, `app/`).
- [x] Шаг 1: Подключение боевых коннекторов (Neon DB + Auth).
- [x] Шаг 2: Установка парсера выгрузок (xlsx/zod) и подключение драйвера Neon Serverless.
- [x] Шаг 3: Автоматический расчет правил аудита на реальных файлах.
- [x] Шаг 4: Развертывание дашборда и экспорт PDF с рекомендациями для подрядчика.
- [x] Шаг 4.1: Универсальный парсер выгрузок Директа (CSV с `;` и запятыми, Windows-1251/UTF-8, любые заголовки).
- [x] Шаг 4.2: Интеллектуальный AI-аудит на Gemini 3.8 Flash + расширение до 6 правил сливов.
- [x] Шаг 4.3: Интерактивная инфографика (Recharts): Donut распределения бюджета, Bar Chart устройств, интерактивный калькулятор окупаемости в чистой светлой теме.
- [x] Шаг 5: Экраны авторизации и профиля (`/sign-in`, `/sign-up`, `/forgot-password`, `/dashboard`), контекст пользователя `useUser`, связывание аудитов с профилем.
- [x] Шаг 5.1: Устранение утечки данных (Data Leakage) в личном кабинете — реестр выгрузок изолирован по `userId`.
- [x] Шаг 5.2: Проверка и ужесточение Row Level Security (RLS) в API.
- [x] Шаг 5.3: Очистка брендинга и стека.
- [x] Шаг 5.4: Тестовый аккаунт и переключатель тарифов для тестирования.
- [x] Шаг 5.5: Полная адаптивность мобильной версии.
- [x] Шаг 6: Глубокая аналитика поисковых запросов, автоматическая минусация мусорных фраз и AI-блеклист площадок РСЯ.
- [x] Шаг 7: Биллинг, Расширенная 5-уровневая Тарифная сетка и White-label Коммерческие PDF-отчеты.
- [x] Шаг 7.1: Безопасность авторизации, Скрытие тестовых кнопок и Серверная валидация паролей.
- [x] Шаг 7.2: Расширенный блок «Что проверяет независимый движок Cransys».
- [x] Шаг 7.3: Полнофункциональная Панель Администратора (`/app/admin`):
  - Полноценный CRUD пользователей (`/api/admin/users`): блокировка, удаление, смена пароля, смена email, ручное изменение тарифов и начисление лимитов.
  - Конфигуратор тарифов (`/api/admin/tiers`): ручное редактирование цен (₽), лимитов отчетов и флагов возможностей (Direct API, White-label, AI).
  - Реальная финансовая аналитика по базе зарегистрированных пользователей.
  - Изоляция маршрутов: Главному администратору закрыт доступ в клиентский кабинет, доступна выделенная кнопка перехода в `/admin`.
- [x] Шаг 7.4: Гейтинг отчетов и Суперюзер Sandbox:
  - Неоплаченные пользователи видят только поверхностные метрики (Индекс здоровья, сумма сливов, расход), детальный отчет заблюрен и заблокирован до оплаты.
  - Тестовый пользователь (`TESTER_ADMIN`) имеет неограниченный доступ с мгновенным переключателем любого тарифа без оплаты.
- [x] Шаг 7.5: Киберзащита, подтверждение Email, надежное хранение истории аудитов и светлая тема.
- [x] Шаг 8.1: Боевая транзакционная почта через Яндекс SMTP (`smtp.yandex.ru:465` от `cransys@yandex.ru`):
  - Сервис отправки `lib/email/mailer.ts` с шаблонами фирменных писем.
  - Отправка 6-значного кода при регистрации (`/api/auth/register`) и повторном запросе (`/api/auth/resend-code`).
  - Оповещение о безопасности при смене пароля (`/api/auth/reset-password`).
- [x] Шаг 8.2: Прямая интеграция с Яндекс.Директ по OAuth:
  - Инициация OAuth в Яндекс ID (`/api/direct/auth`).
  - Обработка callback и получение `access_token` (`/api/direct/callback`).
  - Персистентное хранение подключений в Neon DB + локальный fallback (`lib/db/direct-connections-store.ts`).
  - Статус подключения и отключение (`/api/direct/status`).
  - Запуск аудита кампаний по API в 1 клик (`/api/direct/run-audit`).
  - Интерактивная карточка `DirectConnectCard` в дашборде.
- [x] Шаг 8.4: Исправление и стабилизация процесса регистрации пользователей.
- [x] Шаг 8.5: Комплексная доработка UI/UX и правовой базы платформы:
  - **Админка:** Человекочитаемый формат даты регистрации (`ДД.ММ.ГГГГ, ЧЧ:ММ:СС`) и отслеживание поля «Последний вход» (`lastActive`).
  - **Единый Footer:** Подвал выведен на всех страницах платформы (`/`, `/dashboard`, `/admin`, `/legal/*`, `/sign-in`, `/sign-up`, `/forgot-password`).
  - **Рефакторинг подвала:** Убран блок тарифов и шумный блок возможностей из подвала, 3-колоночная чистая компоновка по стандартам 152-ФЗ РФ.
  - **Глобальная кнопка «Тарифы»:** Размещена в шапке сайта на всех страницах, включая Личный кабинет (`/dashboard`).
  - **100% Синхронизация тарифов и Идеальная геометрия карточек:** Карточки в свернутом виде имеют строго одинаковую высоту и ровное выравнивание кнопок.
  - **Ликвидация пустого пространства:** Устранены избыточные отступы между сеткой тарифов и подвалом.
  - **Правовой комплаенс 152-ФЗ:** Созданы страницы `/legal/privacy`, `/legal/terms`, `/legal/consent` с прямой адресацией.
- [x] Шаг 8.6: Фирменный Фавикон, Векторный Логотип и Полная Техническая SEO-оптимизация:
  - **Фирменный Favicon & Logo:** Интегрирован аутентичный логотип Cransys (синяя стилизованная буква 'C', защитный щит, мишень и красный луч радара) в `public/favicon.svg`, `app/icon.svg`, `components/Logo.tsx`, `Header.tsx`, `Footer.tsx`, `/sign-in`, `/sign-up`.
  - **Dynamic OpenGraph Previews:** Создан `app/opengraph-image.tsx` с автогенерацией брендированных карточек 1200x630 для социальных сетей и мессенджеров (Telegram, VK, WhatsApp).
  - **Technical SEO & Sitemap:** Внедрен `app/sitemap.ts` (динамический XML Sitemap) и `app/robots.ts` (правила индексации для Яндекса и Googlebot с защитой приватных разделов `/admin` и `/api`).
  - **Web App Manifest:** Сформирован `app/manifest.ts` для PWA/мобильных ярлыков и адаптивного оформления.
  - **Schema.org JSON-LD:** Развернуты структурированные микроданные `Organization`, `WebApplication` и `BreadcrumbList` для расширенных сниппетов в поиске.
- [x] Шаг 8.7: Управление Соцсетями и Контактами в Панели Администратора и Динамический Footer:
  - В панели `/app/admin` создана вкладка **«Соцсети и контакты»** с управлением Telegram, VK, YouTube, VC.ru, Habr, WhatsApp и кастомными ссылками.
  - Возможность включать/отключать видимость каждого канала в футере, изменять URL, названия и всплывающие описания.
  - Интерактивный Live Preview футера прямо в админке перед сохранением.
  - Хранилище настроек `lib/db/site-settings-store.ts` и API-эндпоинты `/api/settings` (GET/PUT) с сохранением в JSON/Память.
  - Сквозной Footer динамически подтягивает активные соцсети и email службы поддержки.
- [x] Шаг 8.8: Обновление фирменного логотипа, айдентики и 4-колоночной сетки подвала:
  - **Чистый векторный знак без текста бренда:** Создан высокоточный SVG-логотип на основе чертёжной инженерной сетки Blueprint, ферменного каркаса буквы 'C' и красного геодезического вектора (`components/Logo.tsx`, `public/favicon.svg`, `app/icon.svg`).
  - **Фирменная типографика CRANSYS:** Бренд переведен на единый выверенный геометрический гротеск капсом (`font-extrabold text-[#003882] tracking-[0.14em] uppercase`) на всех страницах и компонентах сайта (Шапка, Футер, Авторизация, Личный кабинет, Панель Admin, OpenGraph карточки).
  - **4-колоночный Footer:** Блок «Сообщества и медиа» вынесен в отдельную структурированную колонку между разделами «Навигация» и «Правовая информация», что полностью устранило смещение верстки.
- [x] Шаг 8.9: Полная внутренняя оптимизация (On-Page SEO), Аналитика и Центр управления SEO в Панели Администратора:
  - **Мобильное меню Header:** Развернуто адаптивное бургер-меню для мобильных устройств, устраняющее переполнение и обрезку кнопок на экранах смартфонов.
  - **On-Page SEO и FAQ Контент:** Внедрен интерактивный блок частых вопросов `components/FaqSection.tsx` с разметкой `FAQPage` в JSON-LD для расширенных сниппетов (Rich Snippets) в Яндексе и Google.
  - **Интеграция систем аналитики и вебмастеров:** Реализован `components/AnalyticsProvider.tsx` для неблокирующей инициализации Яндекс.Метрики (с Вебвизором 2.0), Google Analytics 4 (gtag.js) и метатегов верификации `yandex-verification` / `google-site-verification`.
  - **Вкладка «SEO, Аналитика и Вебмастера» в Админке (`/app/admin`):**
    - Статус-карточки активности Яндекс.Метрики, GA4, Яндекс.Вебмастера и Search Console.
    - Управление номерами счетчиков аналитики и флагом Вебвизора.
    - Ввод кодов подтверждения прав владения сайтом для поисковиков.
    - Редактор метатегов сайта: Meta Title, Meta Description с подсчетом символов, Keywords и селектор индексации `robots`.
    - Поля для вставки пользовательских скриптов и пикселей в `<head>` и `<body>`.
    - Мгновенное сохранение через API `/api/settings` с глубоким слиянием параметров.
- [x] Шаг 8.10: Управление файлами Cookie и Обеспечение Конфиденциальности по 152-ФЗ РФ + Оптимизация отступов 4-колоночного Footer:
  - **Оптимизация отступов подвала (`components/Footer.tsx`):**
    - Устранены избыточные межколоночные разрывы между «Навигацией» и «Сообществами», «Сообществами» и «Правовой информацией». Подвал получил строгую, компактную и ровную геометрию в рамках `max-w-7xl mx-auto`.
  - **Правовая база по 152-ФЗ:**
    - Создана развернутая страница «Политика использования файлов cookie» (`/legal/cookies`) с классификацией категорий (технические, аналитические, маркетинговые), правовыми основаниями, регламентом отзыва согласия и контактными данными DPO/поддержки.
    - В подвал добавлена прямая интерактивная кнопка **«Управление cookie»**, вызывающая модальное окно настроек в любое время.
  - **Интерактивный Баннер и Модальное окно выбора (`components/CookieConsentBanner.tsx`):**
    - Всплывающий баннер с мягкой анимацией, возможностью принять все файлы, согласиться только на необходимые (технические) или открыть детальные настройки.
    - Модальное окно с описанием каждой категории и независимыми переключателями.
    - Сохранение выбора в `localStorage` и `document.cookie` на настраиваемый срок (по умолчанию 365 дней).
  - **Блокировщик трекеров до получения согласия (`components/AnalyticsProvider.tsx`):**
    - Реализован принцип Prior Consent: Яндекс.Метрика, Google Analytics и сторонние скрипты не инициализируются, пока пользователь не даст явное согласие на аналитические cookie.
    - Реактивная шина событий через `useSyncExternalStore` (`lib/consent-client.ts`): при принятии согласия в баннере скрипты активируются немедленно без перезагрузки страницы.
  - **Центр управления 152-ФЗ и Аналитика Согласий в Панели Администратора (`/app/admin`):**
    - Новая вкладка **«152-ФЗ и Cookie»** (`activeTab === 'privacy'`).
    - Карточки статистики: Всего показов баннера, Принято всё, Только технические, Выборочно.
    - Журнал аудита согласий (Audit Trail): таблица последних фиксаций согласий с маскированным IP-адресом (для соблюдения тайны персональных данных), типом выбора и временем.
    - Настройки баннера: включение/выключение показа, включение автоблокировки скриптов, кнопка быстрого отказа, редактирование заголовка и основного текста, срок действия согласия в днях.
  - **API-эндпоинт аудита (`/api/legal/cookie-consent`):**
    - `POST` — анонимизированная фиксация выбора согласия пользователя с маскированием IP.
    - `GET` — выгрузка агрегированной статистики и журнала для панели администратора.
- [ ] Шаг 9.0 (Запланировано): Реальная сквозная продуктовая аналитика и воронка конверсий в Админке (см. `/docs/08_FUNNEL_TELEMETRY_SPEC.md`):
  - Создание таблицы `telemetry_events` в PostgreSQL (Neon).
  - Неблокирующий сборщик `navigator.sendBeacon` на эндпоинт `/api/telemetry/event`.
  - Захват UTM-меток и агрегация воронки: `Визиты ➔ Аудиты ➔ Тарифы ➔ Регистрации ➔ Оплаты`.
  - Подключение реальных графиков в панели администратора `/app/admin`.

---

## 🔑 Чек-лист внешних сервисов (Все 100% Free Tiers БЕЗ привязки карты)
1. **Neon.tech (База данных PostgreSQL):** `DATABASE_URL` — Подключено.
2. **Яндекс SMTP (`cransys@yandex.ru`):** `SMTP_USER`, `SMTP_PASS` — Подключено для боевой отправки писем.
3. **Яндекс OAuth (Директ API):** `YANDEX_CLIENT_ID`, `YANDEX_CLIENT_SECRET` — Архитектура и эндпоинты готовы.
4. **ЮKassa (Прием платежей):** Интеграция API оплаты и автоматическое обновление статуса `hasPaid`.

---

## ⚠️ Риски и блокеры
1. **Яндекс OAuth Client Secret:** После завершения создания приложения в Яндекс ID необходимо добавить переменные `YANDEX_CLIENT_ID` и `YANDEX_CLIENT_SECRET` в настройках Vercel и AI Studio.
