# 📜 История промптов и задач Cransys

Данный файл фиксирует все ключевые задачи и промпты пользователя для отслеживания контекста и эволюции проекта.

---

## Промпт №1 — 2026-09-15
**Задача от пользователя:**
> "Ведешь разработку в knyavik-stack/cransys. действуешь по инструкции. сначала наведи порядок в репозитории создав необходимую иерархию папок. далее по плану с моим согласованием."

**Выполненные действия:**
1. Синхронизированы метаданные проекта `metadata.json` и `app/layout.tsx`.
2. Создан файл инструкций `.cursorrules`.
3. Создана иерархия папок по технической карте v2:
   - `app/` (лендинг, авторизация, дашборд, API)
   - `components/` (ui, audit, dashboard, layout)
   - `db/` (Drizzle ORM схема для Neon PostgreSQL)
   - `lib/` (ядро правил аудита `lib/audit/`, парсеры, R2 клиент)
   - `tests/` (структура фикстур и юнит-тестов)
   - `maket/` (дизайн-система и правила UX)
   - `.github/workflows/` (CI/CD пайплайны)
4. Развернуты файлы ведения статуса: `_СТАТУС.md`, `changelog.md`, `_ПРОМПТЫ_ИСТОРИЯ.md`.
5. Сформирован план следующих шагов для согласования.

---

## Промпт №2 — 2026-09-15
**Задача от пользователя:**
> "погоди, а как мне это в мой пустой репозиторий на гитхабе отправить?"

**Выполненные действия:**
1. Предоставлена пошаговая инструкция по синхронизации проекта с репозиторием `knyavik-stack/cransys`: через нативный интерфейс Google AI Studio (в 2 клика) и через Git CLI.

---

## Промпт №3 — 2026-09-15
**Задача от пользователя:**
> "мне надо автоматически. но вариант 1 сейчас говорит: Sync to GitHub. A new GitHub repository will be created. Changes from Google AI Studio will be synced to GitHub, and vice versa"

**Выполненные действия:**
1. Разъяснен принцип работы нативного GitHub Sync коннектора: создание/связывание репозитория и включение двусторонней синхронизации.
2. Предоставлен сценарий действий для пустого репозитория.

---

## Промпт №4 — 2026-09-15
**Задача от пользователя:**
> "ты не видишь что он мне предлагает создать новый репо. но он у меня уже создан на гитхабе."

**Выполненные действия:**
1. Разъяснено техническое ограничение интерфейса AI Studio Build: модальное окно поддерживает только создание нового репозитория через GitHub API.
2. Предложено прямое и быстрое решение (удалить пустой репо на GitHub, чтобы AI Studio создала его сама под тем же именем и привязала синк).

---

## Промпт №5 — 2026-09-15
**Задача от пользователя:**
> "Failed to create GitHub repository, Insufficient permissions to create the GitHub repository. Please ensure the AI Studio GitHub App is installed and has correct permissions."

**Выполненные действия:**
1. Локализована причина ошибки: GitHub App не имеет прав на создание репозиториев в организации `knyavik-stack`.
2. Предоставлена четкая инструкция по включению прав в настройках GitHub Applications / Organization.

---

## Промпт №6 — 2026-09-15
**Задача от пользователя:**
> "так, дальше какие коннекторы надо настроить и где еще что надо мне создать руками. конкретно какие api тебе нужны и как их достать"

**Выполненные действия:**
1. Составлен приоритизированный реестр внешних сервисов с прямыми инструкциями получения API-ключей на 100% бесплатных тарифах (Neon, Clerk, Cloudflare R2, Upstash, ЮKassa).
2. Разъяснены правила безопасного добавления секретов через панель Secrets в AI Studio.

---

## Промпт №7 — 2026-09-15
**Задача от пользователя:**
> "Cloudflare R2 требует ввести платежные данные. я не буду этого делать. какой выход?"

**Выполненные действия:**
1. Cloudflare R2 полностью исключен из цепочки требований: никаких карт и скрытых списаний.
2. Реализована стратегия Zero-Card Storage: On-Demand генерация PDF + сохранение структуры отчетов в Neon PostgreSQL (где карта НЕ требуется).
3. Архитектура изолирована через провайдер хранения, что позволяет при желании подключить любой S3 в будущем без переписывания логики.

---

## Промпт №8 — 2026-09-15
**Задача от пользователя:**
> "готово" (Ключи Neon и Clerk добавлены в Secrets)

**Выполненные действия:**
1. Установлены библиотеки `xlsx`, `zod`, `@neondatabase/serverless`.
2. Реализован парсер выгрузок Яндекс.Директа `lib/parser/excel-parser.ts` с поддержкой Мастера отчетов и статистики.
3. Интегрирован парсер в компонент `DropZone.tsx` и эндпоинт `app/api/audit/route.ts`.
4. Реализован механизм автоматической миграции таблиц Neon PostgreSQL (`/api/db/init`).
5. Написан и проверен юнит-тест `tests/unit/audit-engine.test.ts`.
6. Сборка приложения (`compile_applet`) и линтер подтвердили готовность.

---

## Промпт №9 — 2026-09-15
**Задача от пользователя:**
> "ты конечно приступай к шагу 4, но скажи каким образом мне протестировать сайт? он же на vercel у нас должен быть или как? что мне сделать?"

**Выполненные действия:**
1. Предоставлены ссылки и инструкции по тестированию: текущий live cloud preview по прямой ссылке, а также пошаговая инструкция привязки Vercel к репозиторию `knyavik-stack/cransys`.
2. Реализация Шага 4: генератор ТЗ подрядчику, экспорт отчета в печатный PDF и дашборд истории аудитов.

---

## Промпт №10 — 2026-09-15
**Задача от пользователя:**
> "во первых csv не анализирует : Не удалось распознать структуру отчета Яндекс.Директ. Убедитесь, что в файле есть колонки «Кампания» и «Расход».. Проверьте, что в отчете Яндекс.Директа есть столбцы «Кампания» и «Расход».
> а вот xlsx анализирует, но замечаний в нем не находит. говорит что все нормально. и тут я понял проблему. все выгрузки таких отчетов от клиентов будут разные, с разными полями и метриками. система должна уметь распозновать файл. если надо, давай подключим ai бесплатные модели для оценки. они нам все равно понадобятся, когда мы будем проводить глубокий анализ и настраивать рекламу в кабинете по API. и еще, если смотреть пример mebliron, то это клиента не привлечет вообще. это скудно и скучно. необходимы графики и дашбоды интерактивные. чтоб было понятно что откуда и куда."

**Выполненные действия:**
1. **Универсальный парсер CSV и XLSX:** переработан `lib/parser/excel-parser.ts` с поддержкой разделителей `;`, `,`, `\t`, декодированием Windows-1251 и UTF-8, расширенным словарем синонимов («Расход», «Затраты», «Сумма», «Списания», «Cost», «Кампания», «Campaign», «Группа» и т.д.) и эвристикой определения сетей по CTR.
2. **Расширение движка до 6 правил аудита:** добавлены правила `Rule05CpaAnomaly` (аномально дорогой лид) и `Rule06LowCtrWaste` (слив на мусорных запросах), порог нулевых конверсий снижен до 1200 ₽ для отлова микросливов.
3. **Бесплатный AI-аналитик (`gemini-3.8-flash`):** модуль `lib/ai/direct-analyst.ts` анализирует срез кампаний, формирует экспертное заключение для собственника, находит скрытые проблемы и составляет чеклист директологу.
4. **Интерактивные графики и дашборд (`components/AuditCharts.tsx` на `recharts`):**
   - Donut Chart каналов (красная подсветка доли слива в РСЯ);
   - Bar Chart эффективности устройств (сравнение расхода и конверсий смартфонов vs ПК);
   - Интерактивный калькулятор перераспределения бюджета с ползунком (+X дополнительных целевых лидов в месяц);
   - Карточка AI-инсайтов от Gemini.

---

## Промпт №11 — 2026-09-15
**Задача от пользователя:**
> "Fix the errors in the app: error 0: bun install failed: exit status 1"

**Выполненные действия:**
1. Диагностирована причина сбоя: параллельный запуск системных и дочерних процессов `bun install` в связке с высоким дефолтным сетевым параллелизмом Bun (`network-concurrency 48`), что вызывало блокировку кэша и сокетов к npm-реестру.
2. Создан конфигурационный файл `bunfig.toml` с `network-concurrency = 8` для стабильной загрузки пакетов в облачном контейнере.
3. Остановлены фоновые конфликтующие процессы, очищены временные блокировки.
4. Выполнен чистый запуск `bun install`, успешно установлены все 470 пакетов за 132с и сгенерирован валидный `bun.lock` (132 КБ).
5. Проведена проверка кодовой базы (`lint_applet`), подтверждена целостность проекта без ошибок.

---

## Промпт №14 — 2026-09-15
**Задача от пользователя:**
> "наведи порядок в файлах в репозитории, разложи все как надо по папкам если это необходимо. следуй принципам хорошего программирования. бардак недопустим! также продолжай дальше по плану, после этого сообщи на каком мы этапе и что сделано и что дальше."

**Выполненные действия:**
1. **Наведение идеального порядка в файловой структуре:**
   - Создана директория `docs/` для всей проектной документации и технических спецификаций.
   - Сформирован реестр документации `docs/README.md`.
   - В `docs/` перемещены и структурированы:
     - `06_ANALYTICS_AND_INSIGHTS_SPEC.md`
     - `AI_FUTURE_ROADMAP.md`
     - `07_BILLING_AND_WHITELABEL_SPEC.md`
   - В корне оставлены строго регламентные операционные файлы: `_СТАТУС.md`, `changelog.md`, `_ПРОМПТЫ_ИСТОРИЯ.md`, `.cursorrules`, `.env.example`, `metadata.json`.
2. **Реализация Шага 7 (Биллинг, Тарифы и White-label для агентств):**
   - Разработан компонент `components/PricingModal.tsx` с тарифами Экспресс (990 ₽), PRO (2 990 ₽/мес) и MAX (6 990 ₽/мес), описанием преимуществ и песочницей мгновенной активации.
   - Разработан модуль `components/WhiteLabelSettingsModal.tsx` для сохранения реквизитов агентства/эксперта (название, телефон, Telegram, сайт, экспертное заключение).
   - Интегрирована автоматическая White-label шапка в `components/AuditResults.tsx` с отображением брендинга агентства в отчете и стилями `@media print` для чистой печати клиенту.
   - Создан API-маршрут `app/api/billing/create-payment/route.ts` с поддержкой ЮKassa и Sandbox-режима.
   - Добавлено управление тарифом и брендингом в личном кабинете `app/dashboard/page.tsx`.
3. Обновлены `_СТАТУС.md`, `changelog.md` (v0.8.0) и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №13 — 2026-09-15
**Задача от пользователя:**
> "продолжай двигаться дальше. и надеюсь у тебя записаны дополнения, что мы будем использовать Ai для будущего анализа и рекомендаций и всего что нам нужно в будущем. чтоб не забыть."

**Выполненные действия:**
1. **Зафиксирована долгосрочная AI-стратегия (`AI_FUTURE_ROADMAP.md`):**
   - Модуль AI-аудитора поисковых запросов и минус-слов.
   - Модуль AI-фильтрации мобильных приложений и игровых дорвеев в РСЯ.
   - Будущий модуль AI-генератора конвертящих заголовков и креативов.
   - Будущий модуль AI-автобиддера и предиктивной аналитики LTV/CPA.
   - Будущий модуль автонастройки и оптимизации кабинета через прямой Yandex Direct API.
2. **Реализован Шаг 6 (Deep Analytics & Negative Keywords):**
   - Создан `lib/ai/search-query-analyst.ts` с кластеризацией запросов и автогенерацией списка минус-слов.
   - Создан `lib/ai/rsya-blacklist.ts` для формирования блеклиста площадок в сетях.
   - Создан `components/SearchQueryVisualizer.tsx` для наглядного отображения сливов на семантике и копирования минус-слов в 1 клик.
   - Обновлен парсер `lib/parser/excel-parser.ts` для считывания отчетов по поисковым фразам.
   - Интегрирован AI-анализ запросов в основной API-эндпоинт `/api/audit`.
3. Обновлены `_СТАТУС.md`, `changelog.md` и `_ПРОМПТЫ_ИСТОРИЯ.md`.

**Задача от пользователя:**
> "я зарегистрировался в личном кабинете и у меня там Сохраненные выгрузки есть меблирон. хотя я проводил тестирование по меблирону до того как залогинился. так же проверь RLS и безопасность. так же проверь адаптивность мобильной версии, сейчас много что не умещается и много что плывет не по размеру с накладками. и так же переходи к следующему этапу по плану. сообщи о результате по плану. так же внеси в репозиторий тестовый аккаунт с возможностью переключения тарифа: экспресс, pro, max."

**Выполненные действия:**
1. **Изоляция данных и устранение Data Leakage:** убран зашитый дефолтный демо-отчет из реестра выгрузок личного кабинета. Реестр формируется строго на основе реальных загрузок пользователя.
2. **Проверка RLS и безопасности:**
   - Внедрена изоляция запросов в `/api/audit/history` на основе `x-user-id`.
   - Внедрена проверка прав доступа в `/api/audit/[id]` с запретом чтения чужих приватных отчетов и возвратом `403 Forbidden`.
3. **Адаптивность мобильной версии (320px–420px):**
   - Переработан `AuditResults.tsx`: карточки ключевых метрик, индикатор здоровья кабинета, адаптивные кнопки («Экспорт PDF», «ТЗ подрядчику», «Новый аудит»).
   - Переработан `AuditCharts.tsx`: компактные круговые и столбчатые диаграммы Recharts, адаптивный ползунок окупаемости без наложений.
   - Оптимизированы `DropZone.tsx`, `Header.tsx`, `ContractorTaskModal.tsx` и `Dashboard.tsx`.
4. **Тестовый аккаунт и переключатель тарифов:**
   - В `UserContext` добавлена поддержка тарифов: `EXPRESS`, `PRO`, `MAX`.
   - Добавлен интерактивный виджет переключения тарифов в шапке и в личном кабинете для мгновенного тестирования возможностей всех тарифных планов без привязки карт.
5. **Очистка брендинга:**
   - Полностью удалены упоминания сторонних брендов («Меблирон») и внутреннего стека из UI.
6. **Переход к следующему этапу:**
   - Подготовлена спецификация `06_ANALYTICS_AND_INSIGHTS_SPEC.md` для модуля глубокого анализа поисковых запросов, автоматической генерации минус-слов и фильтрации мусорных площадок РСЯ.

---

## Промпт №9 — 2026-09-15
**Задача от пользователя:**
> "API доступно только для тарифов от pro и выше, в кабинете это должно сообщаться и подталкивать на расширение. еще: тарифная сетка не доработана. у нас должен быть единоразовый експрес отчет - стоимость 399р. експрес это 3 отчета включено. Pro включено 10 отчетов. Max включено 30 отчетов. и пятый тариф Corp стоимость 29900 Ограничение на 500 отчетов. автоматизация аналитики, группировки. придумай что еще... обнови информацию везде, в том числе на главной стиранице. в тестовом аккаунте тоже. проверь сохраняется ли история поисков, чтоб все подсчитывалось и ограничивалось."

**Выполненные действия:**
1. **Архитектура 5-уровневой тарифной сетки (`lib/billing/tiers.ts`):**
   - `EXPRESS_SINGLE` (399 ₽ разово, 1 отчет).
   - `EXPRESS_PACK` (990 ₽ разово, 3 отчета).
   - `PRO` (2 990 ₽/мес, 10 отчетов + Direct API).
   - `MAX` (6 990 ₽/мес, 30 отчетов + White-label PDF + ТЗ).
   - `CORP` (29 900 ₽/мес, 500 отчетов + Автоматизация, группировки, выделенный сервер и менеджер).
2. **Гейтинг Direct API и мотивация к апгрейду:**
   - Прямой API доступ заблокирован для Экспресс-тарифов.
   - В личном кабинете внедрен выделенный промо-блок с призывом перехода на PRO.
3. **Учет лимитов и прогресс-бар отчетов:**
   - В `UserContext` добавлено отслеживание `reportsUsed` и `reportsLimit`, а также метод `isAllowed(feature)`.
   - Интегрирован инкремент отчетов в `DropZone.tsx` и сохранение истории аудитов.
4. **Витрина тарифов на главной странице (`app/page.tsx`):**
   - Развернута адаптивная витрина 5 тарифов с карточками преимуществ, беджами Direct API и модальным окном оформления.
5. **Обновлены компоненты:**
   - `components/PricingModal.tsx`, `components/Header.tsx`, `app/dashboard/page.tsx`, `components/AuditResults.tsx`.


