# 📜 История промптов и задач Cransys

Данный файл фиксирует все ключевые задачи и промпты пользователя для отслеживания контекста и эволюции проекта.

---

## Промпт №25 — 2026-09-19
**Задача от пользователя:**
> "Fix the errors in the app: Could not initialize telemetry_events table: Error: This function can now be called only as a tagged-template function: sql`SELECT ${value}`, not sql("SELECT $1", [value], options)..."

**Выполненные действия:**
1. **Устранение ошибки вызова Neon Serverless SQL драйвера:**
   - Переписаны все прямые SQL-запросы в `lib/db/telemetry-store.ts` и `lib/db/cookie-consent-store.ts` на формат tagged-template `await sql`...``.
   - В `db/index.ts` добавлена отказоустойчивая поддержка выполнения DDL-скриптов с автоматическим определением API драйвера.
2. **Сквозная компиляция и проверка:**
   - Выполнена сборка `compile_applet`, билд прошел успешно без единой ошибки.

---

## Промпт №24 — 2026-09-19
**Задача от пользователя:**
> "подожди с шагом 10. по шагу 9 вопросы: если сейчас в админке я вижу данные и они тестовые, то их необходимо почистить, чтоб я мог с текущего момента отслеживать живые данные и аналитику. мусор убираем. и сообщи мне вообще какие шаги еще остались кратко списком"

**Выполненные действия:**
1. **Полная очистка тестовых и синтетических данных:**
   - Из `lib/db/telemetry-store.ts` полностью удален генератор синтетических событий `getInitialSeedEvents()`.
   - Файл `.data/telemetry-events.json` сброшен в чистое состояние `[]`.
   - Реализована безопасная обработка нулевых состояний воронки (zero-state handling) без деления на ноль и без артефактов.
2. **Инструмент сброса в Админке и API:**
   - Добавлены эндпоинты `DELETE` и `POST (action: 'clear')` в `/api/admin/funnel/route.ts`.
   - В интерфейс вкладки «Воронка» в Панели Администратора добавлена кнопка «Очистить тестовые» с подтверждением.
3. **Формирование четкого списка оставшихся шагов до полного боевого запуска.**

---

## Промпт №23 — 2026-09-19
**Задача от пользователя:**
> "да. двигайся по плану. проверяй ошибки или дублирование функций (если вдруг ранее уже другой партнер сделал). документируй. дай результат!"

**Выполненные действия:**
1. **Реализация Шага 9.0: Сквозная продуктовая телеметрия и динамическая воронка конверсий:**
   - Развернута архитектура продуктового трекинга (`lib/telemetry/types.ts`, `lib/telemetry/tracker.ts`, `lib/db/telemetry-store.ts`, `app/api/telemetry/event/route.ts`, `app/api/admin/funnel/route.ts`).
   - Добавлена таблица `telemetry_events` в `db/schema.ts` с персистентным хранением в Neon PostgreSQL и локальным fallback `.data/telemetry-events.json`.
   - Произведено сквозное инструментирование пользовательских событий (просмотры страниц, запуск аудитов, просмотр цен, выбор тарифов, регистрации, оплаты).
2. **Модернизация вкладки «Воронка и конверсии» в Админке (`app/admin/page.tsx`):**
   - Полный отказ от статических моков в пользу живого API `/api/admin/funnel`.
   - Внедрен фильтр периодов («Сегодня», «7 дней», «30 дней», «Все время») с мгновенным обновлением.
   - Отрисованы 4 ключевые метрики, визуальные прогресс-бары шагов с расчетом дроп-оффа, таблица UTM-источников и интерактивный график динамики по дням.
3. **Верификация и синхронизация:**
   - Успешная компиляция приложения (`compile_applet`) и проверка линтером (`lint_applet`).
   - Обновлены `_СТАТУС.md`, `changelog.md` и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №22 — 2026-09-19
**Задача от пользователя:**
> "я привязал сайт к домену cransys.ru и настроил яндекс метрику, и яндекс вебмастер, гугл аналитику. твоя задача выступить в роли профессионального SEO и проверить сайт на все настройки, внести необходимые изменения на супер качественный рост популярности сайта бесплатно, чтоб индексировался, искался в поиске, везде о нем было слышно. Необходимые настрйоки обязательно делать в админке и они должны применяться в сайте. это строгие требования. ищи самую актуальную информацию по SEO и применяй ее на практике. четко веди документацию. дай мне готовый результат, что"

**Выполненные действия:**
1. **Серверная динамическая сборка метаданных (`app/layout.tsx`):**
   - Интегрирована функция `generateMetadata()`, которая на каждый запрос запрашивает актуальные настройки из `fetchSiteSettingsAsync`.
   - Настройки Title, Description, Keywords, Robots, OpenGraph и теги верификации `yandex-verification` / `google-site-verification` теперь динамически управляются из Панели Администратора и сразу применяются к `<head>`.
2. **Динамический структурированный JSON-LD (Schema.org):**
   - Внедрена полная схема микроразметки (`Organization`, `WebSite`, `WebApplication`, `FAQPage`, `BreadcrumbList`), формируемая сервером с привязкой к `cransys.ru`.
3. **Техническое SEO, Robots.txt и Sitemap.xml (`app/robots.ts`, `app/sitemap.ts`):**
   - Правила индексации обновлены под домен `https://cransys.ru` с выделенными директивами для `YandexBot` и `Googlebot`.
   - В карту сайта включены все публичные и правовые страницы (`/legal/privacy`, `/legal/terms`, `/legal/consent`, `/legal/cookies`).
4. **Центр управления SEO & Webmaster в Панели Администратора (`app/admin/page.tsx`):**
   - Добавлен тулбар быстрого тестирования (`robots.txt`, `sitemap.xml`, ссылки в панели вебмастеров).
   - Встроен **Live Search Snippet Preview** (живой предпросмотр сниппета Яндекса и Google) для визуальной оценки кликабельности в поисковой выдаче.
   - Подсчет символов Title и Description с подсказками по оптимальной длине.

---

## Промпт №21 — 2026-09-18
**Задача от пользователя:**
> "тебе нужно навести порядок в работе с Секретами и админкой. в коде куча данных прописанных руками, но на самом деле они должны браться либо из секретов, либо из админки. твоя задача разобраться и исправить все свои косяки. делай"

**Выполненные действия:**
1. **Аудит безопасности и удаление хардкода учетных данных (`lib/db/users-store.ts`):**
   - Полностью вычищены жестко заданные хэши паролей `passwordHash` из массива `DEFAULT_USERS`.
   - Внедрена обязательная санитизация при записи и чтении локальных данных (`loadLocalFile`, `writeLocalFile`), чтобы пароли и коды подтверждения никогда не попадали в JSON-файлы репозитория.
2. **Аутентификация через переменные окружения (`app/api/auth/login/route.ts`):**
   - Для учетных записей администратора (`ADMIN`) и тестировщика (`TESTER_ADMIN`) пароли сверяются напрямую с переменными `process.env.ADMIN_PASSWORD` и `process.env.TEST_USER_PASSWORD`.
   - Поддерживаются безопасные дефолтные значения при отсутствии переменных в окружении.
3. **API Мониторинга Системного Окружения (`app/api/admin/system-status/route.ts`):**
   - Разработан эндпоинт безопасной проверки наличия всех ключей и переменных (SMTP, Direct OAuth, Database, Admin, Tester, YooKassa) без раскрытия их фактических секретных значений.
   - Проверка реального подключения к Neon PostgreSQL с замером задержки отклика (latency).
4. **Интерактивная вкладка «Секреты / API» в Панели Администратора (`app/admin/page.tsx`):**
   - Добавлена новая вкладка управления с живой кнопкой «Проверить сейчас».
   - Отображение статуса базы данных и структурированной сетки всех переменных окружения.
   - Информационный блок о кибербезопасности и изоляции паролей.

---

## Промпт №20 — 2026-09-18
**Задача от пользователя:**
> "1. ура - заявку одобрили! но : В подключенном кабинете «mebliron» пока не найдено кампаний. Создайте кампанию в Яндекс.Директ или подключите другой кабинет. а вот для knyavik нашел в архиве, но не нашел активную но неоплаченную: В кабинете 2 кампаний с остановленными показами. Все они доступны для комплексного аудита настроек, минус-фраз и рисков. Аналитику не дает, возможно они старые.
> 2. давай подумаем как ограничивать возможности по нашим тарифным планам. Нужны четкие алгоритмы. Например тариф pro. что будет, если пользователь протестирует один кабинет яндекса, отключит и подключит другой? Это приведет к злоупотреблениям.
> 3. да, приступай, дай результат!"

**Выполненные действия:**
1. **Защита от «карусели» кабинетов (Account Carousel Protection):**
   - Разработана система лимитирования уникальных логинов Яндекс.Директ в расчетный месяц: PRO — 1 кабинет, MAX — 5 кабинетов, CORP — 50 кабинетов.
   - Создана таблица `direct_slots_history` в базе данных Neon PostgreSQL с полями `user_id`, `login`, `connected_at`, `month_period`.
   - В точке входа OAuth (`/api/direct/callback`) реализована строгая валидация: если пользователь пытается привязать новый уникальный логин сверх лимита слотов месяца, запрос блокируется с понятным объяснением ошибки в UI.
   - Повторная привязка того же логина или обновление токена не расходует слот.
2. **Списание лимита аудитов (Reports Quota):**
   - В `/api/direct/run-audit` добавлена строгая проверка `reportsUsed < reportsLimit`.
   - После успешного формирования отчета счетчик `reportsUsed` увеличивается на 1 и фиксируется в Neon PostgreSQL.
3. **Отображение состояния слотов в UI:**
   - В `DirectConnectCard` выведен бейдж «Слоты кабинетов: X/Y в мес.» с информацией об использованных логинах.
   - При исчерпании лимита слотов кнопка добавления блокируется с переходом в выбор тарифа.

---

## Промпт №19 — 2026-09-18
**Задача от пользователя:**
> "1. после авторизации в яндекс: Срок авторизации Яндекс ID истек или токен был отозван. Требуется обновить подключение.
> 2. убирай демо-снимки. должны быть только реальные даныне из подклчюенных кабинетов.
> 3. отключить аккаунт сразу не срабатывает, приходится обновлять страницу.
> 4. Подключить/отключить логин дублируется 2 раза в одном блоке - шум. 
> вообще логика интерфейса в блоке такая, что каждый кабинет можно отключить/обновить токен отдельно. не нужна кнопка отключения сразу всех аккаунтов. то есть этот блок как бы содержит закладки для каждого подключения кабинета. а внутри уже данные по каждому кабинету.
> в результате хочу увидеть реальные данные из моего подключенного кабинета."

**Выполненные действия:**
1. **Интерфейс закладок (табов) для каждого кабинета (`components/DirectConnectCard.tsx`):**
   - Устранено дублирование кнопок подключения/отключения: из шапки блока убран весь шум.
   - Спроектирована панель закладок (табов): каждый подключенный кабинет выведен в отдельную аккуратную вкладку с индикатором активности.
   - Внутри вкладки размещены локальные органы управления конкретным кабинетом: кнопка «Обновить токен» и кнопка «Отключить».
   - Убрана лишняя кнопка общего отключения всех кабинетов.
   - Добавлена кнопка «+ Подключить кабинет» в ряду табов (для тарифов с поддержкой мульти-аккаунтов).
2. **Мгновенное отключение кабинета без перезагрузки:**
   - В `lib/db/direct-connections-store.ts` реализовано физическое удаление записи из PostgreSQL Neon и оперативной памяти.
   - В UI состояние списка подключений реактивно обновляется сразу после вызова API, удаленная закладка мгновенно пропадает, а система переключается на оставшийся кабинет либо в чистое состояние подключения.
3. **Исключение всех демо-снимков Меблирон:**
   - Из API-роутов `/api/direct/campaigns` и `/api/direct/run-audit` полностью удалены любые фиктивные данные и моки.
   - Система возвращает и отображает исключительно реальные кампании пользователя из API Яндекс.Директ v5.
   - При ошибке токена или авторизации выводится четкий системный баннер с кнопкой «Обновить токен», а не фиктивные снимки.
4. **OAuth scope `direct:api` и аутентификация в Яндекс ID:**
   - В URL авторизации (`/api/direct/auth`) добавлен явный параметр `scope=direct:api`.
   - В интерфейсе и документации подготовлена понятная инструкция для проверки прав приложения в консоли `oauth.yandex.ru`.

---

## Промпт №18 — 2026-09-18
**Задача от пользователя:**
> "1. авторизация яндекс работает.
> 2. аккаунт яндекс не отключается. в тарифе corp у нас должна быть возможность подключать/отключать несколько кабинетов.
> 3. "В подключенном аккаунте пока нет активных кампаний Яндекс.Директ.", но:
> компании то есть в моем кабинете и они настроены. там просто остановка показов произошла.
> наша система должна показывать все компании в кабинет и анализировать их.
> они же настроены, они же работали или если не работали, но они же настроены.
> мы это все должны учитывать и давать свое резюме или анализ, сообщая пользователю о возможностях.
> 4. мне непонятен вопрос с периодом анализа. как мы его задаем или как мы вообще этим пользуемся? тоже надо реализовать."

**Выполненные действия:**
1. **Мульти-аккаунты и индивидуальное отключение (`lib/billing/tiers.ts`, `lib/db/direct-connections-store.ts`, `/api/direct/status`, `components/DirectConnectCard.tsx`):**
   - В структуру тарифов добавлено свойство `maxConnectedAccounts` (CORP: до 50, MAX: до 5, PRO: 1).
   - База данных и хранилище адаптированы под хранение множественных подключений для одного `userId`.
   - В интерфейсе `DirectConnectCard` выведен список подключенных аккаунтов со статусом, активным логином и кнопкой индивидуального отключения через `DELETE /api/direct/status?connectionId=...`.
   - Добавлена кнопка «+ Кабинет» для подключения дополнительных логинов через всплывающее окно OAuth.
2. **Отображение всех кампаний (включая остановленные):**
   - В API Яндекс.Директ v5 передаются статусы `States: ['ON', 'OFF', 'SUSPENDED', 'ENDED', 'ARCHIVED']`.
   - Кампании с остановленными показами отображаются с бейджем «Остановлена / Приостановлена • Готова к аудиту настроек и минус-фраз».
   - Добавлены фильтры («Все», «Идут показы», «Остановлены», «Архив») и быстрый выбор («Все», «Только активные», «Только остановленные», «Снять выбор»).
   - Движок аудита производит всестороннюю оценку настроек, семантики и рисков перезапуска кампаний.
3. **Реализация выбора периода анализа:**
   - Разработан блок выбора глубины сбора данных: 30 дней, 90 дней (по умолчанию), 180 дней (полгода), 365 дней (год) и произвольный диапазон дат (от/до).
   - Внедрена интерактивная подсказка «Зачем нужен период анализа?», объясняющая, что для остановленных кампаний необходим срез 90–180 дней для захвата исторической статистики.
   - Период учитывается при запуске аудита и выводится в заголовке отчета.
4. **Стабильность и компиляция:**
   - Код успешно прошел линтинг (`npm run lint`) и производственную сборку (`next build`).

---

## Промпт №17 — 2026-09-18
**Задача от пользователя:**
> "верх для мобильной версии не адаптирован: https://www.cransys.ru/dashboard там должно быть аналогично как на главной странице."

**Выполненные действия:**
1. **Унификация и мобильная адаптация шапки (`app/dashboard/page.tsx`, `components/Header.tsx`):**
   - Удалена устаревшая статичная шапка в личном кабинете и подключен сквозной компонент `Header`.
   - Верхняя панель на `/dashboard` теперь полностью совпадает с главной страницей:
     - На смартфонах: логотип `CRANSYS DIRECT`, быстрые кнопки «Тарифы» и «Аудит», бургер-кнопка с раскрывающимся меню (профиль, тарифный бейдж, вход/выход, 152-ФЗ).
     - На десктопе: подсвечивается активная вкладка «Кабинет».
2. **Проверка компиляции и обновление документации:**
   - Проект успешно собран (`next build`). Обновлены `_СТАТУС.md`, `changelog.md` и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №16 — 2026-09-18
**Задача от пользователя:**
> "1. Реализовать мобильную панель навигации (MobileBottomNav) для бесшовного управления с мобильных устройств.
> 2. Обеспечить сохранение настроек сайта и Email поддержки в базе данных PostgreSQL (Neon) без сброса к значениям по умолчанию.
> 3. Оптимизировать сетку вкладок в панели администратора (/admin) без горизонтального скролла.
> 4. Заменить блок 152-ФЗ в личном кабинете на карточку «Остаток проверок».
> 5. Исправить ошибки сборки и статических ресурсов."

**Выполненные действия:**
1. **Мобильная навигация (`components/MobileBottomNav.tsx`):**
   - Интегрирована сквозная плавающая нижняя панель («Аудит», «Тарифы», «Кабинет»/«Админка», «Войти»/«Выйти»).
   - На смартфонах обеспечен мгновенный доступ к ключевым страницам и модальным окнам.
2. **Персистентность настроек в PostgreSQL (`db/schema.ts`, `lib/db/site-settings-store.ts`, `/api/settings`):**
   - Создана таблица `public.app_settings`.
   - Настройки сайта, контактный Email и социальные сети теперь персистентно сохраняются в Neon DB.
3. **Компактная сетка вкладок в админке (`app/admin/page.tsx`):**
   - Реорганизована панель навигации на сетку `grid`, устранена горизонтальная прокрутка.
4. **Метрика «Остаток проверок» в кабинете (`app/dashboard/page.tsx`):**
   - Выведен точный расчет доступного остатка отчетов по текущему тарифу.
5. **Стабилизация сборки и маршрутизации:**
   - Перенесен `icon.svg` в `public/icon.svg`, создан `app/not-found.tsx`, успешная компиляция проекта.

---

## Промпт №15 — 2026-09-18
**Задача от пользователя:**
> "1. После регистрации страница не обновилась если регистрироваться со страницы кабинета.
> 2. В кабинете верхнее меню не адаптировано под мобильную версию и нет кнопки меню. Лучше сделай «кнопки» внизу (как будто в мобильном приложении).
> 3. Если находиться в legal, то не работает кнопка в верху Аудит (не переходит по кнопке).
> 4. Добавить/изменить текст на сайте, там где это необходимо, добавив слова, которые могут быть проиндексированы поисковой системой: Яндекс директ, реклама сайта, раскрутка, seo продвижение и так далее. То есть слова, которые пользователи забивают в поисковую строку.
> 5. В тарифной сетке в блоках после названия тарифа указано в скобках количество отчетов, а ниже указан лимиты, а после цены тоже указаны лимиты. Это дублирование-шум и еще увеличивает размер секторов. И перед ценой много пустого пространства.
> 6. В тарифной сетке не надо писать год 2026. Вместо года напиши Analytics.
> 7. Распиши функции тарифов чуть подробнее, разделив на бОльшее количество пунктов. чтоб пользователю было (и визуально тоже) понятно, что реальные различия между тарифами существенны. Внести изменения в настройки тарифов в админке.
> 8. В секторах блока Что проверяет независимый движок Cransys, иконку ставить перед названием секции, а не над ней."

**Выполненные действия:**
1. **Реактивное обновление кабинета после регистрации:** В `lib/auth/user-context.tsx` добавлен метод `setUserProfile` и слушатели `cransys_user_updated`/`storage`. Регистрация из кабинета (`/sign-up`) теперь моментально обновляет состояние авторизации без рассинхронизации.
2. **Мобильная навигация Bottom Bar (`components/MobileDashboardNav.tsx`):** Создана нижняя панель действий (как в нативных мобильных приложениях) с быстрым доступом к «Аудиту», «API Директ», «Истории», «Тарифам» и «Профилю».
3. **Навигация из раздела Legal:** Исправлена кнопка «Аудит» в шапке `components/Header.tsx`: ссылка заменена на `/#audit-section`, что обеспечивает безошибочный переход к аудиту с любых правовых страниц (`/legal/*`).
4. **Поисковая SEO-оптимизация:** Обновлены `metadata.json`, `app/layout.tsx`, `components/FaqSection.tsx` и тексты главной страницы. Органично добавлены запросы: «Яндекс Директ», «реклама сайта», «раскрутка», «seo продвижение», «контекстная реклама».
5. **Очистка сетки тарифов и устранение дублирования:** Удалены избыточные скобки с количеством отчетов, убраны дублирующиеся лимиты под названиями и после цен, оптимизированы вертикальные отступы карточек.
6. **Брендинг Analytics:** Год «2026» заменен на бренд «Cransys Analytics» в `components/PricingModal.tsx` и `app/page.tsx`.
7. **Детализация и расширение функций тарифов:** В `lib/billing/tiers.ts` существенно расширен перечень возможностей для каждого тарифа (Экспресс, Экспресс Пакет, PRO, MAX White-label, Corporate) для наглядной дифференциации планов. Конфигурации синхронизированы с админкой.
8. **Редизайн блока проверки Cransys:** Иконки векторов проверок размещены в одну строку перед заголовком секции в `app/page.tsx`.

---

## Промпт №1 — 2026-09-15
**Задача от пользователя:**
> "Ведешь разработку в knyavik-stack/cransys. действуешь по инструкции. сначала наведи порядок в репозитории создав необходимую иерархию папок. далее по плану с моим согласованием."

**Выполненные действия:**
1. Синхронизированы метаданные проекта `metadata.json` и `app/layout.tsx`.
2. Создан файл инструкций `.cursorrules`.
3. Создана иерархия папок по технической карте v2: `app/`, `components/`, `db/`, `lib/`, `tests/`, `maket/`, `.github/workflows/`.
4. Развернуты файлы ведения статуса: `_СТАТУС.md`, `changelog.md`, `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №10 — 2026-09-15
**Задача от пользователя:**
> "1. наведи порядок в подвале сайта.
> 2. расширь раздел но сделай компактно "Что проверяет независимый движок Cransys" - там должно быть ясно написано преимущество. призыв посмотреть демо отчет (можно без регистрации, а потом уже призыв купить платный тариф (после регистрации).
> 3. Срочно спрячь Тест-аккаунт. ты чего натворил? у нас вход в тестовый аккаунт это просто вход, но под конкретным логином и паролем котоырй ты должен был записать в сервретные ключи!!!
> 4. впиши в план работ (в документацию): реализовать и весь функционал Админики .../admin. просмотр и управление тарифов и статистикой всех пользователей. ручной сброс статистики или корректировка количества израсходованных лимитам по тарифам, изменение тарифов. так же обобщенная статистика по неавторизированным пользователям (чтоб я мог анализировать, сколько раз пробовали демо и почему не купили подписку). в админке так же разработать аналитику с дашбордами, графиками, статистикой."

**Выполненные действия:**
1. Наведение порядка в подвале сайта (`components/Footer.tsx`).
2. Расширение раздела УТП «Что проверяет независимый движок Cransys» на главной странице.
3. Сокрытие тестовых кнопок и вынос учетных данных в защищенные серверные секреты.
4. Разработка начального каркаса панели администратора (`/admin`).

---

## Промпт №11 — 2026-09-15
**Задача от пользователя:**
> "1. Главному администратору не нужен доступ в личный кабинет на сайте!.
> 2. сделай так, чтоб я в админке мог: блокировать пользователей, удалять, менять пароль, менять емайл. конфигурировать тарифы вручную.
> 3. сейчас в админке вымышленные данные, но нет данных пользователей которые зарегистрировались.
> 4. сейчас можно войти в кабинет по любому паролю (это недопустимо). нужна полноценная комбинация: регистрация, восстановление пароля, вход.
> 5. если пользователь зарегистрируется или не зарегистрировался и у него нет оплаченной подписки, то сможет воспользоваться анализом файла, система ему покажет только поверхностные цифры и отчет будет заблюрен и недоступен для скачивания. только после оплаты соответствующего тарифа доступны полные функции в соответствии с тарифом.
> 6. У тестового пользователя (только у него) должно все работать при выборе любого тарифа без оплаты (это как суперюзер с предоплаченными тарифами для тестирования возможностей тарифов.)"

**Выполненные действия:**
1. **Персистентное хранилище данных (`lib/db/users-store.ts` и `lib/db/tiers-store.ts`):**
   - Реализована надежная база пользователей и конфигурации тарифов с сохранением изменений.
   - Финансовые KPI и статистика в админке теперь рассчитываются строго по реальным пользователям из базы.
2. **Полноценный CRUD пользователей в админке (`/api/admin/users` и `/app/admin/page.tsx`):**
   - Блокировка и разблокировка пользователей в 1 клик с запретом авторизации.
   - Удаление пользователей с модальным окном подтверждения.
   - Изменение email, пароля, имени, лимитов проверок, израсходованных отчетов и выручки.
   - Ручное создание новых пользователей.
3. **Конфигуратор тарифов (`/api/admin/tiers`):**
   - Ручное изменение цен (₽), лимитов отчетов и включение/выключение модулей.
4. **Разграничение ролей:**
   - Администратору закрыт доступ в клиентский дашборд.
5. **Защита отчетов (Paywall-гейтинг):**
   - Неоплаченные пользователи видят поверхностные метрики, детальный аудит заблокирован.
6. **Суперюзер:**
   - Тестовому пользователю открыт доступ ко всем функциям и тарифам без оплаты.

---

## Промпт №12 — 2026-09-15
**Задача от пользователя:**
> "Fix the errors in the app. Uncaught ChunkLoadError: Loading chunk app/page failed."

**Выполненные действия:**
1. Проведена диагностика и очистка кэша чанков сборки Next.js 15.
2. Перезапущен dev-сервер приложения.
3. Проведена полная верификация типов и сборки (`lint_applet` и `compile_applet` успешно пройдены).
4. Обновлены `_СТАТУС.md`, `changelog.md` и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №13 — 2026-09-16
**Задача от пользователя:**
> "1. История аудитов пустая после нескольких проверок.
> 2. Пароль пользователя должен выполнять требования безопасности. не менее 8 символов. латиница, большие маленькие и цифры. Подтверждение емайл. ты забыл про киберзащиту и безопасность?
> 3. Регистрация так и не работает. навигация работает, а вот реальная запись в БД не происходит.
> 4. на сайте только светлая тема. раздел Оцените глубину аудита на эталонном отчете и подвал должны быть в светлой теме.
> Веди документацию. Сообщи на какой мы стадии, что сделано, что не сделано и какой следующий шаг по плану?"

**Выполненные действия:**
1. **Персистентное хранилище аудитов (`lib/db/audit-store.ts`):**
   - Реализовано персистентное сохранение результатов аудита в Neon PostgreSQL с отказоустойчивым локальным JSON-хранилищем.
   - Во все вызовы аудита (`DropZone.tsx` и `page.tsx`) добавлена передача заголовков `x-user-id` и `x-user-email`.
   - В `/api/audit/history` и `/api/audit/[id]` обеспечена фильтрация и выдача истории и детальных отчетов для каждого пользователя.
2. **Кибербезопасность паролей (`lib/auth/password-validator.ts`):**
   - Внедрены требования: от 8 символов, только латиница, минимум одна строчная, одна заглавная буква и цифра.
   - Добавлен визуальный интерактивный валидатор критериев пароля в режиме реального времени на странице `/sign-up` и в API сброса пароля.
3. **Реальная запись пользователей в БД и подтверждение Email:**
   - Реализована 2-этапная регистрация: сохранение учетной записи в базу данных со статусом ожидания верификации (`emailVerified: false`) и выдача 6-значного кода безопасности.
   - Созданы API эндпоинты подтверждения кода (`/api/auth/verify-email`) и повторной отправки (`/api/auth/resend-code`).
   - Все зарегистрированные пользователи сохраняются в Neon PostgreSQL (таблица `users`) и локальном хранилище, отображаясь в панели администратора (`/admin`) со статусом подтверждения email.
4. **100% Светлая тема:**
   - Блок «Оцените глубину аудита на эталонном отчете» переведен в чистую светлую тему.
   - Подвал сайта (`components/Footer.tsx`) полностью переведен в светлую тему.
5. **Верификация и сборка:**
   - Устранены ошибки типов, успешно пройдены `compile_applet` и `lint_applet`.
   - Обновлены файлы трекинга `_СТАТУС.md`, `changelog.md`, `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №14 — 2026-09-16
**Задача от пользователя:**
> "у нас хостинг сейчас вот сюда идет с гитхаба https://cransys.vercel.app/
> что мне нужно сделать в яндексе?
> по поводу Боевая отправка почты. давай просто для начала через конкретный почтовый ящик сделаем, например у меня есть cransys@yandex.ru. скажи что от меня надо.
> 1. скажи куда конкретно вставить пароль для почты. в чате нельзя пересылать.
> 2. что выбрать: Какое приложение хотите создать?
> хост страницы, на которой разместится кнопка или виджет авторизации."

**Выполненные действия:**
1. **Боевая почта Яндекс SMTP (`cransys@yandex.ru`):**
   - Установлен и настроен `nodemailer` с параметрами `smtp.yandex.ru:465` (SSL).
   - Создан модуль `lib/email/mailer.ts` с шаблонами фирменных писем подтверждения email и оповещения о смене пароля.
   - Подключена отправка писем в `/api/auth/register`, `/api/auth/resend-code` и `/api/auth/reset-password`.
   - В `.env.example` задекларированы переменные `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`.
2. **Прямое подключение к Яндекс.Директ по OAuth:**
   - Созданы роуты `/api/direct/auth` (инициация с CSRF-защитой) и `/api/direct/callback` (обмен кода на `access_token` и `refresh_token`).
   - Создано персистентное хранилище токенов `lib/db/direct-connections-store.ts` с поддержкой Neon DB и локального fallback.
   - Создан эндпоинт `/api/direct/status` и `/api/direct/run-audit` для сканирования кампаний в 1 клик через Direct API v5 Reports.
   - В личный кабинет добавлен интерактивный компонент `DirectConnectCard.tsx`.
3. **Верификация:**
   - Пройдены `lint_applet` (0 ошибок, 0 предупреждений) и `compile_applet` (100% зеленый билд).

---

## Промпт №15 — 2026-09-16
**Задача от пользователя:**
> "не, так не пойдет. ты сказал, что я могу добавить в секреты ai studio и ты сделаешь так, что при следующей синхронизацией с гитхаб это все появится в vercel. делай"

**Выполненные действия:**
1. Разработан скрипт `scripts/sync-env-to-vercel.mjs` и npm-команда `npm run sync:vercel`. Скрипт считывает локальные переменные окружения и автоматически отправляет их в проект Vercel через Vercel API v10 без раскрытия в репозитории.
2. Создан GitHub Actions воркфлоу `.github/workflows/sync-env-to-vercel.yml` для автоматической синхронизации при пуше в `main`.
3. Добавлен диагностический эндпоинт `/api/admin/diagnostics` для проверки статуса секретов и SMTP без раскрытия их содержимого.
4. После добавления `VERCEL_TOKEN` выполнена прямая синхронизация переменных в Vercel: все ключи (`SMTP_*`, `YANDEX_*`, `ADMIN_*`, `TEST_USER_*`) созданы в проекте Vercel (HTTP 201 Created).
---

## Промпт №17 — 2026-09-17
**Задача от пользователя:**
> "1. в админке исправь формат поля Регистрация 2026-09-17T06:43:50.309Z, добавь поле - последний вход.
> 2. у нас на сайте подвал только на главной странице, так не должно быть. подвал должен быть на всех страницах.
> 3. из подвала убери раздел тарифы. и скомпонуй по принципам хорошего дизайна.
> 4. Кнопка Тарифы в меню, должна быть на всех страницах.
> 5. в всплывающем окне сетки тарифов больше информации о тарифах чем на главной странице в сетке тарифов. информация должна быть одинаковая. НО на главной странице, чтоб не загромождать, сделай раскрывающийся список (подробнее/скрыть) для каждого тарифа или общей сетки.
> 6. не забывай все документировать."

**Выполненные действия:**
1. **Панель Администратора (`/app/admin`):**
   - Форматирование даты: заменен ISO-формат на понятный русский формат `ДД.ММ.ГГГГ, ЧЧ:ММ:СС`.
   - Добавлена колонка «Последний вход» (`lastActive`) в таблице пользователей.
   - Серверные маршруты авторизации (`/api/auth/login`) и подтверждения email (`/api/auth/verify-email`, `users-store.ts`) теперь сохраняют и обновляют точное время последней активности.
2. **Сквозной глобальный Footer:**
   - Подвал (`components/Footer.tsx`) подключен ко всем страницам приложения: главной странице, личному кабинету (`/dashboard`), админ-панели (`/admin`), правовым разделам (`/legal/*`), а также страницам авторизации (`/sign-in`, `/sign-up`, `/forgot-password`).
3. **Рефакторинг и компоновка подвала:**
   - Удален блок тарифов из подвала.
   - Выполнена чистая компоновка по стандартам дизайна 2026: логотип, УТП, стек технологий, навигация, правовой комплаенс 152-ФЗ РФ и контакты.
4. **Кнопка «Тарифы» в меню шапки:**
   - В компонент `Header.tsx` внедрена кнопка «Тарифы», доступная на всех страницах и экранах (десктоп и мобильное меню), открывающая сетку тарифов.
5. **Синхронизация информации о тарифах:**
   - Все параметры (названия, лимиты, цены, бейджи Direct API и White-label, полный перечень преимуществ) между `PricingModal.tsx` и `app/page.tsx` синхронизированы на 100%.
   - В карточки тарифов на главной странице добавлен аккордеон («Все возможности (N) ▼ / Скрыть ▲») для сохранения компактного внешнего вида.
6. **Правовая база:**
   - Созданы отдельные страницы `/legal/privacy`, `/legal/terms`, `/legal/consent` и модальное окно `LegalModal.tsx`.
---

## Промпт №18 — 2026-09-17
**Задача от пользователя:**
> "1. в кабинете в топе нет кнопки Тарифы.
> 2. убери в подвале раздел Возможности. все ссылки в нем просто ведут к аудиту. это бесполезный шум.
> 3. на главной странице в сетке тарифов блоки в свернутом положении должны быть одинакового размера.
> 4. на главной странице после тарифной сетки много пустого пространства
> 5. не понял зачем в самом низу сайта ссылки на отдельные страницы политики и соглашений, если есть в подвале Правовой блок и связь. если объяснишь смысл и убедишь что это так по каким то правилам, то Ок.
> 6. объясни как должна собираться реально в админке информация для раздела "Воронка и конверсии", из каких данных, что надо внедрить? обьясни.
> не забывай документировать"

**Выполненные действия:**
1. **Личный кабинет (`/app/dashboard/page.tsx`):**
   - В верхнюю навигационную панель дашборда интегрирована кнопка «Тарифы» с открытием модального окна `PricingModal`.
2. **Очистка подвала (`components/Footer.tsx`):**
   - Удален раздел «Возможности», создававший шум ссылками на одну и ту же форму аудита.
   - Устранено дублирование юридических ссылок: подвал структурирован на 3 аккуратные колонки с прямыми ссылками на регламенты 152-ФЗ и контакты.
3. **Идеальное выравнивание тарифов (`app/page.tsx`):**
   - Карточки тарифов в свернутом виде приведены к единой высоте (`items-stretch`, контейнеры бейджей `h-[46px]`, цен `h-[48px]`, описания и фичей `min-h-[82px]`). Кнопки CTA выстроены строго по одной горизонтальной оси.
4. **Устранение пустого пространства:**
   - Оптимизированы отступы секции тарифов (`pt-10 pb-8`) и подвала (`py-10`), устранив пустоту.
5. **Юридическое обоснование и архитектура воронки:**
   - Предоставлено детальное правовое обоснование обязательности прямых статических ссылок для 152-ФЗ, банковских шлюзов и модерации Яндекс.Директа.
   - Подготовлена детальная инженерная спецификация сбора событий для раздела «Воронка и конверсии» в админке.

---

## Промпт №20 — 2026-09-17
**Задача от пользователя:**
> "1. что это такое и как пользоваться: Dynamic OpenGraph Cards (app/opengraph-image.tsx): Автоматическая генерация красивой превью-карточки 1200x630 с УТП для ссылок в Telegram, VK, WhatsApp и соцсетях.
> 2. в подвале (или посмотри где удобнее разместить) забыли ссылки на соцсети. настрйока этих ссылок сделай в админке."

**Выполненные действия:**
1. **Разъяснение технологии Dynamic OpenGraph Cards:**
   - Подробно объяснен принцип работы стандарта Open Graph, механизма `@vercel/og` (Satori / Yoga layout engine) в App Router Next.js и автоматического рендеринга баннеров 1200x630 при отправке ссылок в Telegram, WhatsApp, VK, MAX и соцсетях.
2. **Управление Соцсетями и Контактами в Панели Администратора:**
   - В `/app/admin` создана новая вкладка **«Соцсети и контакты»** (`activeTab === 'settings'`).
   - Реализована гибкая конфигурация: включение/выключение каналов (Telegram, VK, YouTube, VC.ru, Хабр, WhatsApp и произвольные пользовательские), редактирование ссылок, названий, всплывающих подсказок.
   - Добавлен интерактивный Live Preview футера прямо в админке перед сохранением.
   - Подключение настроек к хранилищу `lib/db/site-settings-store.ts` через эндпоинты `/api/settings` (GET/PUT).
3. **Динамический Footer (`components/Footer.tsx`):**
   - Футер подтягивает актуальные ссылки из API и рендерит аккуратные иконки соцсетей со ссылками на сообщества.
4. **Обновление документации:**
   - Актуализированы `_СТАТУС.md`, `changelog.md` (версия 1.5.0) и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №21 — 2026-09-17
**Задача от пользователя:**
> "1. замени favicon на новый и лого. возьми только лого без надписи бренда. а название таким шрифтом подбери и поменяй на сайте имя бренда. на всех страницах сделай, а то ты не везде поменял.
> 2. Мы в сообществах и медиа: - ты не удобно разместил, теперь все съехало. сделай это столбцом между Навигацией и Правовой информацией.
> 3. что это такое и как пользоваться: Dynamic OpenGraph Cards?"

**Выполненные действия:**
1. **Новый Векторный Логотип и Фавикон (Без надписи внутри знака):**
   - На основе чертёжной инженерной сетки Blueprint, ферменного контура буквы 'C' и красного геодезического вектора создан высокоточный SVG-знак в `components/Logo.tsx`, `public/favicon.svg` и `app/icon.svg`.
2. **Сквозная Типографика Бренда CRANSYS:**
   - Название бренда приведено к премиальному капсу с разрядкой (`CRANSYS`, `tracking-[0.14em]`, `#003882`) на всех страницах и компонентах (Header, Footer, Sign-in, Sign-up, Dashboard, Admin, OpenGraph-image).
3. **4-колоночный Footer (Идеальная геометрия без смещений):**
   - Блок «Сообщества и медиа» вынесен в отдельный самостоятельный столбец между «Навигацией» и «Правовой информацией». Верстка стала идеально сбалансированной.
4. **Обновление документации:**
   - Обновлены `_СТАТУС.md`, `changelog.md` (версия 1.5.1) и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №22 — 2026-09-17
**Задача от пользователя:**
> "1. проведи полную Внутреннюю оптимизацию (On-Page SEO).
> 2. Аналитика и вебмастера (обязательная внешняя настройка) Панели вебмастеров (Google Search Console / Яндекс.Вебмастер) — инструменты для отслеживания индексации, ошибок сайта и позиций в поиске. Системы аналитики (Google Analytics / Яндекс.Метрика) — сервисы для анализа поведения пользователей, трафика и конверсий. все необходимые настройки SEO (Technical SE, On-Page SEO, google, yandex) должны быть доступны в админке!!!
> 3. в мобильной адаптации не умещается меню верхняя часть сайта.
> документируй все по инструкции."

**Выполненные действия:**
1. **Мобильное адаптивное меню (`components/Header.tsx`):**
   - Интегрировано полнофункциональное всплывающее бургер-меню для экранов смартфонов. Устранено выпадение и обрезка кнопок «Тарифы», «Вход» и «Аудит».
2. **Полная On-Page SEO оптимизация и FAQ-разметка:**
   - Развернут компонент `components/FaqSection.tsx` с ответами на ключевые поисковые интенты (аудит Директа, безопасность 152-ФЗ, экономия бюджета).
   - Внедрена разметка `Schema.org FAQPage` в `app/layout.tsx` для генерации расширенных сниппетов (Rich Snippets) в результатах выдачи Яндекса и Google.
3. **Инжектор аналитики и верификации (`components/AnalyticsProvider.tsx`):**
   - Создан клиентский провайдер неблокирующей инициализации Яндекс.Метрики с Вебвизором 2.0, Google Analytics 4, метатегов верификации вебмастеров и кастомных скриптов в `<head>` / `<body>`.
4. **Центр управления SEO, Аналитикой и Вебмастерами в Админке (`/app/admin`):**
   - В панели администратора добавлена вкладка **«SEO, Аналитика и Вебмастера»** (`activeTab === 'seo'`).
   - Реализован статус-бар подключения всех 4 систем.
   - Управление номерами счетчиков Яндекс.Метрики и GA4.
   - Поля ввода кодов подтверждения прав Яндекс.Вебмастера и Google Search Console.
   - Редактор метаданных страницы (Meta Title, Description, Keywords, Robots meta).
   - Редактор произвольных HTML/JS вставок (VK Реклама, пиксели, онлайн-чаты).
   - Интеграция с API `/api/settings` (глубокое слияние и сохранение).
5. **Обновление документации:**
   - Обновлены `_СТАТУС.md`, `changelog.md` (версия 1.6.0) и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №23 — 2026-09-17
**Задача от пользователя:**
> "1. в обычной веб версии сайта, в подвале расстояние между четырьмя блоками слишком большое из-за этого сам подвал шире чем вся структура страницы: между Навигация и Сообщества, Между Сообщества и Правовая информация.
> 2. полностью организуй процесс "Управление файлами cookie и обеспечением конфиденциальности" по всем нормам и правилам в России. все что необходимо для управления и аналитики - добавляй в админку. документируй!"

**Выполненные действия:**
1. **Устранение избыточных отступов в подвале (`components/Footer.tsx`):**
   - Устранены широкие разрывы между четырьмя колонками подвала. Контент зафиксирован в границах `max-w-7xl mx-auto` с гармоничными интервалами.
2. **Юридический комплаенс 152-ФЗ РФ и страница Политики (`app/legal/cookies/page.tsx`):**
   - Создана исчерпывающая страница политики использования cookie с описанием необходимых (технических), аналитических и маркетинговых файлов, правовых оснований и способов отзыва согласия.
3. **Баннер согласия и модальное окно выбора (`components/CookieConsentBanner.tsx`):**
   - Реализован всплывающий баннер с опциями согласия со всеми cookie, выбора только необходимых или вызова детального модального окна.
   - Выбор сохраняется в `localStorage` и `document.cookie` на настраиваемый срок.
4. **Блокировка аналитики и трекеров (Prior Consent, `components/AnalyticsProvider.tsx`):**
   - Яндекс.Метрика и Google Analytics не запускаются до получения явного согласия пользователя.
   - Создан `lib/consent-client.ts` с подпиской `useSyncExternalStore` для реактивной мгновенной инициализации аналитики без перезагрузки страницы при клике «Принять».
5. **Центр 152-ФЗ и Аналитика согласий в Админке (`/app/admin`):**
   - Внедрена вкладка **«152-ФЗ и Cookie»** (`activeTab === 'privacy'`).
   - Реализованы карточки конверсий согласий (Total, Все, Необходимые, Выборочно).
   - Развернут журнал аудита согласий (Audit Trail) с маскированием IP-адресов.
   - Добавлены настройки баннера (включение, автоблокировка скриптов, быстрый отказ, тексты, срок действия).
6. **API учета согласий (`/api/legal/cookie-consent`):**
   - Обработка и сохранение анонимизированных согласий и выдача статистики для админки.

---

## Промпт №24 — 2026-09-17
**Задача от пользователя:**
> "у тебя была задача: полностью организуй процесс "Управление файлами cookie и обеспечением конфиденциальности" по всем нормам и правилам в России. все что необходимо для управления и аналитики - добавляй в админку. документируй! 
> проверь, все ли ты сделал, если нет, доделай."

**Выполненные действия:**
1. **Комплексная ревизия и аудит комплаенса 152-ФЗ:**
   - Проверено сквозное функционирование всех звеньев цепочки: Баннер ➔ Модальное окно ➔ `document.cookie` / `localStorage` ➔ Реактивный провайдер аналитики `AnalyticsProvider` ➔ API `/api/legal/cookie-consent` ➔ Панель администратора `/app/admin` (вкладка «152-ФЗ и Cookie»).
2. **Устранение ошибок линтера и типов:**
   - Разрешены предупреждения React 19 / Next.js по `react-hooks/set-state-in-effect` с помощью `useSyncExternalStore` в `lib/consent-client.ts` и ленивой инициализации состояния.
   - Синхронизированы типы `CookieBannerSettings` и `CookieConsentRecord` в `lib/settings/types.ts` и админ-панели.
3. **Финальная сборка и валидация:**
   - `lint_applet` и `compile_applet` завершены со 100% успехом (0 ошибок).
4. **Актуализация проектной документации:**
   - Обновлены `_СТАТУС.md`, `changelog.md` (версия 1.7.0) и `_ПРОМПТЫ_ИСТОРИЯ.md`.

---

## Промпт №25 — 2026-09-17
**Задача от пользователя:**
> "1. возвращаемся к проверки и работоспособности яндекс аутентификации при подключении если в тарифе доступно. я тестирую на тестовом аккаунте который внесен в секреты. форма авторизации яндекса показывается и визуально проходит ок. Но при закрытии формы авторизации я не вижу в личном кабинете информации что у меня подключено и сколько каких кабинетов подключено, какие дам компании настроены, по каким сделать анализ и так далее. двигаемся в этом направлении. и что там у нас дальше по это теме сообщай, будем дорабатывать.
> документируй по инструкции.
> 2. форму кукисов сделай светлую и чуть меньше по высоте, сделай ее компактной но не сильно выделяющейся и не мешающей, но чтоб пользователь принял все.. у нас основной цвет сайта светлый!"

**Выполненные действия:**
1. **OAuth Popup Flow и синхронизация Яндекс.Директ:**
   - В эндпоинте `/api/direct/auth` реализована передача `userId` и флага `popup=1` через base64url-состояние и cookies.
   - В эндпоинте `/api/direct/callback` реализована отправка сообщения через `window.opener.postMessage` с закрытием всплывающего окна и мгновенным обновлением статуса в личном кабинете.
   - В хранилище `lib/db/direct-connections-store.ts` внедрен fallback для корректной привязки соединения к текущему пользователю в Neon PostgreSQL.
2. **Получение рекламных кабинетов (`/api/direct/accounts`):**
   - Интеграция с Direct API v5 методом `agencyclients`.
   - Определение типа аккаунта (прямой рекламодатель или агентство) и вывод селектора кабинетов при наличии нескольких субклиентов.
3. **Получение и выбор кампаний (`/api/direct/campaigns`):**
   - Запрос кампаний через API v5 с передачей заголовка `Client-Login`.
   - Отображение статусов («Идут показы», «Остановлена»), счетчиков кликов и показов, дат и типов.
   - Интерактивные чекбоксы для выбора конкретных кампаний для аудита, кнопки «Выбрать все» и «Снять выбор».
   - Предусмотрен демонстрационный снимок кампаний для тестирования, если аккаунт Директа новый или пустой.
4. **Таргетированный аудит кампаний (`/api/direct/run-audit`):**
   - Прием массива `campaignIds` и логина кабинета.
   - Запуск движка аудита `defaultAuditEngine`, сохранение отчета в базу данных с привязкой к профилю пользователя и мгновенный вывод результатов в личном кабинете.
5. **Обновление карточки `components/DirectConnectCard.tsx`:**
   - Вывод информации об авторизованном логине и количестве подключенных кабинетов.
   - Селектор переключения кабинетов.
   - Список кампаний с чекбоксами и детальной статистикой.
   - Кнопка «Запустить аудит (выбрано: N)» с прогрессом и индикатором выполнения.
6. **Светлый компактный редизайн Cookie Consent Banner (`components/CookieConsentBanner.tsx`):**
   - Перевод баннера и модального окна на светлую палитру (`bg-white`, `border-slate-200`, `text-slate-800`), соответствующую общей светлой гамме сайта.
   - Уменьшенная компактная высота баннера, не перекрывающая рабочий контент.
   - Привлекательная акцентная синяя кнопка «Принять все» (`bg-blue-600`) для максимизации показателя согласий.
   - Лаконичные светлые кнопки «Настроить» и «Только необходимые».
7. **Верификация и документирование:**
   - Проект успешно скомпилирован (`compile_applet`) без единой ошибки.
   - Обновлены `_СТАТУС.md`, `changelog.md` (версия 1.8.0) и `_ПРОМПТЫ_ИСТОРИЯ.md`.







